apiVersion: v1
kind: Service
metadata:
  name: <SERVICE_NAME>
  namespace: prod
spec:
  type: ClusterIP
  ports:
    - name: http-port
      port: 3000
      targetPort: 3000
  selector:
    app: <SERVICE_NAME>
